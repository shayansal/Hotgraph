[package]
name = "rg-memory-activation"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
rg-core = { path = "../rg-core" }
rg-storage = { path = "../rg-storage" }

[dev-dependencies]
rg-events = { path = "../rg-events" }

[lints]
workspace = true
