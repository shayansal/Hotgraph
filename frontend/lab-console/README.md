# Reality Graph Lab Console

Executive command console for AI lab deployments of Reality Graph.

```bash
npm install
npm run dev
```

The console is currently backed by deterministic fixture data so teams can inspect
retrieval quality, memory health, source trust, latency, cost, incidents, and graph
growth before wiring live API feeds.
