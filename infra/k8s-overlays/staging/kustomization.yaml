apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../k8s

configMapGenerator:
  - name: hotgraph-environment
    behavior: create
    literals:
      - HOTGRAPH_ENVIRONMENT=staging
      - HOTGRAPH_PRODUCTION_CLAIM=false
      - HOTGRAPH_WRITER_REPLICAS=1
